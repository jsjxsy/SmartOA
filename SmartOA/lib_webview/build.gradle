apply from: '../lib.common.gradle'
android {
    defaultConfig {
        versionCode 1
        versionName "1.0"
        ndk {
            abiFilters "armeabi"

        }
    }

}

dependencies {
    api fileTree(dir: 'libs', include: ['*.jar'])
}
